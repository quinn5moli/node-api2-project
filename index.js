// require your server and launch it here
const server = require('./api/server');

const PORT = 4040;

//Start Server Here

server.listen(PORT, () => {
    console.log(`\n** server listening on port ${PORT}**`
    .america
    );
})